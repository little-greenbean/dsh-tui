# dsh-tui publishes to the official npm registry. The user's global registry
# may point at a read-only mirror (e.g. registry.npmmirror.com), which rejects
# `npm adduser` / `npm publish`; this project-level override keeps login and
# publish on registry.npmjs.org without changing the global mirror config.
registry=https://registry.npmjs.org
